{% extends ':Front:layout.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="well">


                    <h1>Oops! An Error Occurred</h1>

                    {% if status_code == 403 %}
                        {{ 'user_has_not_permissions'|trans }}
                    {% endif %}

                    <h2>The server returned a "{{ status_code }} {{ status_text }}".</h2>
                </div>
            </div>
        </div>
    </div>


{% endblock %}